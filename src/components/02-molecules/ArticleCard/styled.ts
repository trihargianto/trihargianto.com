import styled from "styled-components";

import { SPACING_IN_PX_UNIT } from "../../00-tokens/spacing";

export const Article = styled.article`
  margin-bottom: ${SPACING_IN_PX_UNIT.XS}px;
  margin-top: ${SPACING_IN_PX_UNIT.XS}px;
`;
