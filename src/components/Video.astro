---
interface Props {
  webm: string;
  mp4: string;
}

const { webm, mp4 } = Astro.props;
---

<video loop muted playsinline autoplay width="100%">
  <source src={webm} type="video/webm" />
  <source src={mp4} type="video/mp4" />
</video>
